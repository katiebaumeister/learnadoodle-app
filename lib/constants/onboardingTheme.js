/**
 * Onboarding modal branding — keep in sync across steps (PlanningMode, LearningContext, etc.)
 * Sky: primary actions / progress / checkbox fill. Purple: headings & body copy (app accent).
 */
export const ONBOARDING_SKY = '#85C4F2';
/** Default app purple (e.g. TodayScheduleCard, FamilyPanel accents) — lighter than deep violet-800 */
export const ONBOARDING_TEXT_PURPLE = '#8B7CF6';
